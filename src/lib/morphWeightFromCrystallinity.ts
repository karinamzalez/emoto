/**
 * Maps a crystallinity value (0–1) to a morph target influence weight.
 * Currently an identity — placeholder for future easing curves.
 */
export function morphWeightFromCrystallinity(crystallinity: number): number {
  return crystallinity
}
